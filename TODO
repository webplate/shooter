better classes for projectiles

add trajectory object, weapon object?

collision should return culprit

??HOW to describe level ?identities as dicts of name, sprites (if none render font), weapons, trajectory...

idea: one thread for positions, one thread for drawing
